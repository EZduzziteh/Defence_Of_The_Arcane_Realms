namespace Gameplay
{
    using UnityEngine;

    public class Projectile_Spawn_Point : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}